// https://docs.cypress.io/api/introduction/api.html

describe('Login Scenario', () => {
  it('Visits the app root url', () => {
    cy.visit('/');
    // cy.find('h2o')
  });
  it('Visits the app root url', () => {
    cy.visit('/');
    // cy.find('h2o')
  });
  it('Visits the app root url', () => {
    cy.visit('/');
    // cy.find('h2o')
  });
});
